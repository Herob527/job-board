---
import { actions } from "astro:actions";
import Layout from "../layouts/Layout.astro";
import LoginForm from "../components/LoginForm";

const test = () => {
	const t2 = "";
};

const login = await Astro.session?.get("login");
console.log(login);
---

<Layout>
	<h1>Astro</h1>
	<div>
		<section class="test">
			<h1>test</h1>
		</section>
		<LoginForm client:load />
	</div>
</Layout>
